## 使用标曲QC的小靶标DDF流程

### 标曲
1、需要15个标线样本，2个QC样本(HQC[48]和LQC[76])。
2、QC数据会生成在groupedQC文件夹下，用作后续项目分析时计算Accuracy。
### 样品
1、可在表格里筛选特定的标曲。先点Stop，然后筛选标曲，最后点Start。



20250331, HQ

1）修改headMessage模板，自动关联浓度单位和报告模板路径，ddf中调用对应报告模板。
2）删除报告《forAutoReport.R》中无关信息，报告模板和单位从headMessage中调用。
3）修改报告内容（核对细节信息）。
4）小靶标和大靶标分析流程统一，通过headMessage中的质控条件控制。
5）flow-8界面更新：
    a、在“标线选择”中，3选一分支流程：Plan A = 使用默认标线；Plan B = 使用手动筛选的标线；Plan C = 直接生成报告。

