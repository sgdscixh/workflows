---
output: word_document
---



```{r setup, include=FALSE}
library(dplyr)
library(readxl)
library(kableExtra)
library(knitr)
library(pander)
library(rmarkdown)


options(knitr.kable.NA = "")
knitr::opts_chunk$set(echo = FALSE, warning=FALSE)


## create quantification
f_quant <- paste0(l.a$outDir, "/Table S5-Quantification Results.xlsx")
qc_cpds <- l.a$get_table_quant(l.a=l.a, overwrite=TRUE, filename = f_quant)
l.a$cpds <- qc_cpds$cpds
l.a$data_qc <- qc_cpds$qc
l.a$has_qc <- !is.null(l.a$data_qc)

if(!l.a$has_qc) file.rename(f_quant, paste0(l.a$outDir, "/Table S4-Quantification Results.xlsx"))





## headMessage sheet-1 and path
head_msg <- l.a$head_msg
head_path <- l.a$head_path
grp_types <- l.a$grp_type %>% stringi::stri_split(fixed = '|') %>% unlist()
l.a$fig_tag <- ifelse(is.na(l.a$grp_type), "", "-merge")

## Other table path
other_table <- glue::glue("{rep}/{rpt}/others/Table.xlsx", rep=l.a$repDir, rpt=l.a$rpt_flow)



## sampleInfo sheet-1
if (l.a$has_qc){
  spl_name_qc <- l.a$spl_info %>% filter(sample_type == "QC") %>% select(sample_name) %>% .[[1,1]]
  fig_qc_0 <- sprintf("%s/figures/XIC/QC/%s.png",  l.a$resDir, spl_name_qc)
}else{
  spl_name_qc <- "HQC"
  fig_qc_0 <- sprintf("%s/../../Calibrator/results/figures/XIC/QC/%s.png",  l.a$resDir, spl_name_qc)
}

spl_name_sp <- l.a$spl_info %>% filter(sample_type == "SPL") %>% select(sample_name) %>% .[[1,1]]
fig_sp_0 <- sprintf("%s/figures/XIC/SPL/%s.png", l.a$resDir, spl_name_sp)



# cover info
project_code = l.a$head_msg["项目编号", 1]
project_name = l.a$head_msg["项目名称", 1]
custom_inst = l.a$head_msg["客户单位", 1]
custom_name = l.a$head_msg["客户姓名", 1]


```


```{r cover, child=glue::glue("{rep}/{rpt}/rmd/cover_word.Rmd", rep=l.a$repDir, rpt=l.a$rpt_flow)}
```


```{r tables, child=glue::glue("{rep}/{rpt}/rmd/tables_word.Rmd", rep=l.a$repDir, rpt=l.a$rpt_flow)}
# 样本基本信息

# 实验信息

## 仪器及试剂
### 所用主要试剂
### 实验仪器

## 实验方法
### 代谢物提取
```


```{r method, child=glue::glue("{rep}/{rpt}/rmd/method.Rmd", rep=l.a$repDir, rpt=l.a$rpt_flow)}
```







### 校准曲线

采用前文所描述的方法对校准溶液进行UHPLC-MRM-MS分析。校准曲线如Figure S1-Calibration所示，y表示目标化合物和对应内标物的峰面积之比，x表示目标化合物的浓度(nmol/L)。若某一校准浓度Accuracy超出80-120%范围，则将该浓度校准点排除。


\newpage

### 方法检出限及定量限

将校准溶液依次稀释2倍后进行UHPLC-MRM-MS分析，通过其信噪比来计算方法的检出限和定量限。方法 最低检出限(LLOD)定义为信噪比为3时所对应的化合物浓度，方法最低定量限(LLOQ)定义为信噪比为10时所 对应的化合物浓度(US FDA guideline for bioanalytical method validation)。


```{r acc-rsd-cn, eval=l.a$has_qc}
"
### 方法精密度及准确度

方法的精密度通过QC样品重复进样的标准相对偏差(RSD)来评估。准确度通过测得浓度与理论浓度的百分比值来评估。
" %>% pander()

```



## 实验结果
```{r results, include=FALSE}

xic_cap1 <- glue::glue("{grp}标准品溶液 {spl} XIC图", spl=spl_name_qc, grp=grp_types[1])
xic_cap2 <- glue::glue("{grp}样品 {spl} XIC图", spl=spl_name_sp, grp=grp_types[1])
xic2_cap1 <- glue::glue("{grp}标准品溶液 {spl} XIC图", spl=spl_name_qc, grp=grp_types[2])
xic2_cap2 <- glue::glue("{grp}样品 {spl} XIC图", spl=spl_name_sp, grp=grp_types[2])

```


### 色谱分离

标准品溶液及样本提取离子色谱图(EIC)如图`r l.a$fig_count()`-`r l.a$fig_count()`和图`r l.a$fig_count()`-`r l.a$fig_count()`所示，从中可以看出：

  1）本项目所采用分析方法，所有目标化合物均呈现出对称的色谱峰；
  
  2）很好地实现了各个目标化合物的色谱分离；
  
  3）目标化合物在生物样品与标准品溶液中的保留时间及色谱峰形均无明显差异。


```{r xic11, fig.cap=xic_cap1, out.width="100%"}

fig_qc_0 %>% gsub(l.a$grp_type, grp_types[1], .) %>% include_graphics()

```

```{r xic12, fig.cap=xic_cap2, out.width="100%"}

fig_sp_0 %>% gsub(l.a$grp_type, grp_types[1], .) %>% include_graphics()

```

```{r xic21, fig.cap=xic2_cap1, out.width="100%"}

fig_qc_0 %>% gsub(l.a$grp_type, grp_types[2], .) %>% include_graphics()

```

```{r xic22, fig.cap=xic2_cap2, out.width="100%"}

fig_sp_0 %>% gsub(l.a$grp_type, grp_types[2], .) %>% include_graphics()

```

\newpage

### 方法定量参数

```{r acc-rsd, include=FALSE}
## create calibrator
f_acc_cal <- paste0(l.a$outDir, "/Table S3-Calibration.xlsx")
data_cal <- l.a$get_table_calib(l.a, filename=f_acc_cal)

llod_0 <- min(data_cal$LLOD) %>% sprintf("%.4g",.)
llod_1 <- max(data_cal$LLOD) %>% sprintf("%.4g",.)
lloq_0 <- min(data_cal$LLOQ) %>% sprintf("%.4g",.)
lloq_1 <- max(data_cal$LLOQ) %>% sprintf("%.4g",.)




scores <- min(data_cal$`R^2`)


```


目标化合物的定量参数见Table S3，各目标化合物的最低检出限(LLODs)在`r llod_0` - `r llod_1` nmol/L，最低定量限(LLOQs)在`r lloq_0` - `r lloq_1` nmol/L 之间；所有目标化合物的相关系数(R^2)均大于`r scores` ，说明色谱 峰面积与化合物浓度间呈良好的定量关系，可满足靶向代谢组学分析的要求(详细校准曲线见Figure S1-Calibration)。



```{r qc-acc, eval=l.a$has_qc}
acc_max <- 30
rsd_max <- 30
f_acc_rsd <- paste0(l.a$outDir, "/Table S4-Accuracy and RSD.xlsx")
data_rsd <- l.a$get_table_stats(l.a, filename=f_acc_rsd, fill=rsd_max)

n <-  head_msg["QC样品重复进样次数", 1]
rsd_0 <- round(min(data_rsd$`Accuracy[%]`, na.rm=TRUE), 1)
rsd_1 <- round(max(data_rsd$`Accuracy[%]`, na.rm=TRUE), 1)
ratio <- round(100 - nrow(data_rsd[data_rsd$`RSD[%]` %>% grep(">",.),]) / nrow(data_rsd) * 100, 2)

msg_cn <- glue::glue(
  "QC样品的准确度(Accuracy)及标准相对偏差(RSD)见Table S4，QC样品重复进样次数为{n}次。",
  "如表中所示，所有目标化合物的平均准确度在{rsd_0}% - {rsd_1}%之间，",
  "RSD小于{rsd_max}%的代谢物占比为{ratio}%。",
  n=n, rsd_0=rsd_0, rsd_1=rsd_1, acc_max=acc_max, ratio=ratio,rsd_max=rsd_max
)

msg_en <- glue::glue(
  "Table S4 lists the analytical accuracies and relative standard deviations (RSDs) of the QC samples，",
  "with {n} technical replicates. ",
  "The accuracy determined were {rsd_0}% - {rsd_1}% for all the analytes, ",
  "with {ratio}% of the RSDs below {rsd_max}% (n = {n}).",
  n=n, rsd_0=rsd_0, rsd_1=rsd_1, acc_max=acc_max, ratio=ratio,rsd_max=rsd_max
)

pander(msg_cn)

```


以上数据表明，本方法能够在以上所示的浓度范围内，准确可靠地检测出样品中的目标代谢物含量。




## 实验小结

本项目采用UHPLC-MRM-MS/MS的方法，采用内标法，针对样品中的`r paste(l.a$cpds[1:min(length(l.a$cpds), 20)], collapse=', ')`等`r length(l.a$cpds)`种目标代谢物，共计完成`r sum(data_group[2, -1])`例`r l.a$spl_name`样本的分析检测。



```{r spl-type, child=glue::glue("{rep}/{rpt}/rmd/{spl}_end.Rmd", rep=l.a$repDir, rpt=l.a$rpt_flow, spl=l.a$spl_type)}
### 样本中目标代谢物检测结果

```



\newpage

```{r en-end, child=glue::glue("{rep}/{rpt}/rmd/end.Rmd", rep=l.a$repDir, rpt=l.a$rpt_flow)}
## 英文参考步骤（使用时英文方法需根据实际前处理进行调整）

```



```{r copy-data, include=FALSE}
## copy mzml files
d_data_1 <- paste(l.a$outData, grp_types[1], sep="/")
d_data_2 <- paste(l.a$outData, grp_types[2], sep="/")
d_data_1 %>% dir.create(recursive = TRUE)
d_data_2 %>% dir.create(recursive = TRUE)

d_cals_1 <- glue::glue("{d}/../Calibrator", d=l.a$inDir) %>% gsub(l.a$grp_type, grp_types[1], .)
l.a$copy_file(d_base=d_cals_1, d_sub="CAL", patt="mzML$", d_dest=d_data_1)
l.a$inDir %>% gsub(l.a$grp_type, grp_types[1], .) %>% l.a$copy_file(d_sub="SPL", patt="mzML$", d_dest=d_data_1)



d_cals_2 <- glue::glue("{d}/../Calibrator", d=l.a$inDir) %>% gsub(l.a$grp_type, grp_types[2], .)
l.a$copy_file(d_base=d_cals_2, d_sub="CAL", patt="mzML$", d_dest=d_data_2)
l.a$inDir %>% gsub(l.a$grp_type, grp_types[2], .) %>% l.a$copy_file(d_sub="SPL", patt="mzML$", d_dest=d_data_2)

```



```{r copy-extra, include=FALSE, eval=TRUE}
## for small panel

## copy calibrator figures
fig_tag <- l.a$fig_tag
cal_fig <- glue::glue("{res}/figures/Calibrators.best{tag}.pdf", res=l.a$resDir, tag=fig_tag)
new_fig <- glue::glue("{out}/Calibrators.best{tag}.pdf", out=l.a$outDir, tag=fig_tag)
file.copy(cal_fig,l.a$outDir)
file.rename(new_fig, paste0(l.a$outDir,'/Figure S1-Calibration.pdf'))


# xic_fig <- glue::glue("{res}/figures/XIC-SPL{tag}.pdf", res=l.a$resDir, tag=fig_tag)
# xic_new <- glue::glue("{out}/XIC-SPL{tag}.pdf", out=l.a$outDir, tag=fig_tag)
# file.copy(xic_fig,l.a$outDir)
# file.rename(xic_new, paste0(l.a$outDir,'/Figure S2-Sample XIC.pdf'))


## copy other tables for small
d_others <- glue::glue("{rep}/{rpt}/others/", rep=l.a$repDir, rpt=l.a$rpt_flow)
file_cp <- grep('Table S', list.files(d_others), value=T)
file.copy(paste0(d_others, file_cp), l.a$outDir)

```



